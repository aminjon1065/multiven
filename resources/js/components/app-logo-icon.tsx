export default function AppLogoIcon({ className }: { className?: string }) {
    // return <img src={logo} className={`bg-background ${className}`} alt="Logo" />;
    return <span className={`bg-background text-4xl font-bold text-blue-600 dark:text-blue-500 ${className}`}>N</span>;
}
